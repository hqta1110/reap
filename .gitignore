# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# reap specfic
artifacts/
# ...but the observation caches are committed under calibration/ (see its README):
# they are small, expensive to recompute, and what makes pruning minutes.
scratch/
*.log
.env
.uv_cache/
wandb/
api-key.txt
prod_env/credentials.conf
merging_env_6/
merging_env_7/
pruning_env_4/
pruning_env_5/
*.sqllite
credentials.json
config/wildbench_prod_env_*
cmd.sh

# per-machine paths (copy from pipeline/reap.env.example)
pipeline/reap.env

# scheduler runtime state + downloaded calibration corpora
pipeline/scheduler/.jobs.lock
pipeline/scheduler/.push.lock
pipeline/scheduler/RUNNING.txt
pipeline/scheduler/DONE.txt
data/
